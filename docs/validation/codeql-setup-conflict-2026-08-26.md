# CodeQL Status — 2026-08-26

The repository uses GitHub Code Scanning **Default Setup** as its primary static analysis engine. This configuration covers `actions`, `javascript`, `typescript`, and `python` automatically.

- **Status**: Configured and active.
- **Engine**: CodeQL.
- **Mode**: Default Setup (GitHub-managed).

The repository-managed advanced workflow was retired to prevent configuration conflicts. All security alerts are managed directly through the GitHub Security tab.

